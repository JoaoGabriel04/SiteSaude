services:
  backend:
    build: ./backend
    ports:
      - "7000:7000"
    env_file:
      - ./backend/.env.production  # com as URLs do Supabase
    # sem volumes, sem command override — usa o CMD do Dockerfile